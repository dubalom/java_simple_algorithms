def array_separator(array):
    temp = []
    if len(array) < 2:
        return array
    for i in range(len(array)):
        temp.append([i])
    return temp

def function_sort(array):
    array = array_separator(array)
    while i < len(array):
        pass
        
            
        
    
